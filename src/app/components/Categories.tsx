export default function Categories() {
    return (
      <div
        className="relative bg-cover bg-center h-[600px] flex items-center justify-end mt-36"
        style={{
          backgroundImage: "url('/images/Group 115.png')",
          backgroundSize: "contain", // Ensure image fits entirely within the container
          backgroundRepeat: "no-repeat", // Avoid image repetition
        }}
      >
        {/* Other content */}
      </div>
    );
  }
  